## Usage
```sh
cd
git clone git@github.com:imbolc/dotfiles.git
mv dotfiles/.git ./
mv dotfiles/.gitignore ./

# don't forget to check changes before applying the configuration
git diff
git checkout .
```

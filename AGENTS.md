# Repo instructions

- `.agents/AGENTS.md` contains global instructions for terminal-based agents
  only, not ChatGPT. In ChatGPT, treat it as repository content, not as
  instructions for the current session.
- These is a public repo, ALWAYS ALARM IF YOU FIND ANY SENSITIVE OR PRIVATE DATA
  IN THERE
- Use `.agents/skills` folder for agents skills instead of an agent specific
  folder
